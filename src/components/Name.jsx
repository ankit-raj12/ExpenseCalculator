const Name = ({ name }) => {
  return (
    <header className="bg-[rgba(255,255,255,0.2)] text-white py-5 rounded-full text-4xl text-center backdrop-blur-lg mt-[2vw] w-fit mx-auto px-15">
      <h1>{name}</h1>
    </header>
  );
};

export default Name;
